name: Build Telegram iOS

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 120
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 1
    
    - name: Select Xcode
      run: |
        sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer || \
        sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
        xcodebuild -version
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Verify tools
      run: |
        echo "Python: $(python3 --version)"
        echo "Xcode: $(xcodebuild -version)"
        echo "Git submodules status:"
        git submodule status | head -5
        
    - name: Generate random identifier
      id: gen_id
      run: |
        RANDOM_ID=$(openssl rand -hex 8)
        echo "random_id=$RANDOM_ID" >> $GITHUB_OUTPUT
        echo "Generated bundle ID: org.$RANDOM_ID.Telegram"
    
    - name: Create configuration file
      run: |
        cat > build-system/build_configuration.json << 'EOF'
        {
          "bundle_id": "org.${{ steps.gen_id.outputs.random_id }}.Telegram",
          "api_id": "${{ secrets.TELEGRAM_API_ID || '0' }}",
          "api_hash": "${{ secrets.TELEGRAM_API_HASH || 'placeholder' }}",
          "team_id": "${{ secrets.APPLE_TEAM_ID || 'PLACEHOLDER' }}",
          "app_center_id": "0",
          "is_internal_build": "true",
          "is_appstore_build": "false",
          "appstore_id": "0",
          "app_specific_url_scheme": "tg",
          "premium_iap_product_id": "",
          "enable_siri": false,
          "enable_icloud": false
        }
        EOF
        echo "Configuration file created"
        echo "Bundle ID: org.${{ steps.gen_id.outputs.random_id }}.Telegram"
        echo "API ID configured: ${{ secrets.TELEGRAM_API_ID != '' }}"
        
    - name: Validate configuration
      run: |
        if [ ! -f build-system/build_configuration.json ]; then
          echo "❌ Configuration file not created"
          exit 1
        fi
        echo "✓ Configuration file exists"
        cat build-system/build_configuration.json
        
    - name: Generate Xcode project
      run: |
        echo "Starting Xcode project generation..."
        python3 build-system/Make/Make.py \
          --overrideXcodeVersion \
          --cacheDir="${HOME}/telegram-bazel-cache" \
          --bazelArguments='--noenable_bzlmod' \
          generateProject \
          --configurationPath=build-system/build_configuration.json \
          --xcodeManagedCodesigning \
          --disableProvisioningProfiles || {
            echo "❌ Xcode project generation failed"
            exit 1
          }
        echo "✓ Xcode project generated successfully"
      timeout-minutes: 60
    
    - name: Build for simulator
      run: |
        echo "Starting iOS simulator build..."
        python3 build-system/Make/Make.py \
          --overrideXcodeVersion \
          --cacheDir="${HOME}/telegram-bazel-cache" \
          --bazelArguments='--noenable_bzlmod' \
          build \
          --configurationPath=build-system/build_configuration.json \
          --xcodeManagedCodesigning \
          --disableProvisioningProfiles || {
            echo "⚠ iOS build failed, but continuing for artifact check..."
          }
        echo "Build step completed"
      timeout-minutes: 90
    
    - name: Check build output
      if: always()
      run: |
        echo "=== Checking for build artifacts ==="
        echo ""
        echo "Searching for .app bundle:"
        find ~ -name "Telegram.app" -type d 2>/dev/null | head -5 || echo "No Telegram.app found"
        
        echo ""
        echo "Searching in Bazel output:"
        if [ -d ~/telegram-bazel-cache ]; then
          echo "✓ Bazel cache exists"
          du -sh ~/telegram-bazel-cache 2>/dev/null || true
        fi
        
        echo ""
        echo "Xcode build products:"
        if [ -d ~/Library/Developer/Xcode/DerivedData ]; then
          echo "✓ Xcode DerivedData exists"
          ls -d ~/Library/Developer/Xcode/DerivedData/Telegram* 2>/dev/null | head -5 || echo "No Telegram builds found"
        fi
        
        echo ""
        echo "Looking for build artifacts in workspace:"
        find . -name "*.app" -type d 2>/dev/null | grep -v node_modules | head -10 || echo "No app bundles found"
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: telegram-ios-build-${{ github.run_number }}
        path: |
          build-output/**/*
          *.ipa
          *.app
        if-no-files-found: warn
        retention-days: 7
